# Project rules for Claude

## Git & GitHub

- Never run `gh pr merge` or any equivalent merge command. PRs are merged by the user in the GitHub UI only.
- Never push directly to `main`. All changes go through a PR branch.
- Always open a PR after pushing a branch — never leave a branch unreviewed.
